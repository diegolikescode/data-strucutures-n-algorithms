#include "queue.h"

#include <stdio.h>

// Cria uma queue com um elemento e apontando pra NULL
Node* queue_init(int value) { return NULL; }

// Coloca um elemento no topo da queue
void queue_push(Node* list, int value) {}

// Remove alguma coisa do topo da queue
Node* queue_pop(Node* list) { return NULL; }

// retorna o topo da queue
int queue_top(Node* list) { return 0; }

// imprime a queue para a tela.
void queue_print(Node* list) {}
