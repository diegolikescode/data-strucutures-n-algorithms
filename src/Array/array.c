#include "array.h"

// ARRAY
// int capacity;
// int used;
// int* data;

void print(int* array, int size) {}

Array* init() { return NULL; }

void push(Array* arr, int value) {}

int pop(Array* arr) { return NULL; }

int shift(Array* arr) { return 0; }

Array* concat(Array* arr1, Array* arr2) { return NULL; }
